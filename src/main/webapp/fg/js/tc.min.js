function popWin(a) {
    var d, e, b = 9e3,
    c = !1,
    f = $("#" + a),
    g = f.width(),
    h = f.height(),
    i = $(".main_dialog_hd_move"),
    k = document.documentElement,
    l = ($(document).width() - f.width()) / 2,
    lt = $(document).width() - f.width() - 10,
    m = (k.clientHeight - f.height()) / 2;
	f.addClass("mov");
    i.mousedown(function(a) {
        c = !0,
        d = a.pageX - f.offset().left,
        e = a.pageY - f.offset().top,
        f.addClass("drag-star");
    }),
    i.mouseup(function() {
        c = !1;
        f.removeClass("drag-star");
    }),
    f.mouseup(function() {
        c = !1
        f.removeClass("drag-star");
    }),
    $(document).mousemove(function(a) {
        if (c) {
            var b = a.pageX - d;
            0 >= b && (b = 0),
            b = Math.min(k.clientWidth - g, b) - 5;
            var i = a.pageY - e;
            0 >= i && (i = 0),
            i = Math.min(k.clientHeight - h , i) - 5,
			f.removeClass("mov")
            f.css({
                top: i - 72,
                left: b,
            })
        }
    })
}